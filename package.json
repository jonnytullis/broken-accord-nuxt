{
  "name": "broken-accord-nuxt",
  "version": "1.0.0",
  "description": "Broken Accord Official Website",
  "author": "jonathantullis",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt build && nuxt generate",
    "generate:static": "DEPLOY_ENV=STATIC_WEBSITE nuxt build",
    "deploy:static": "push-dir --dir=dist --branch=static_website --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.0.0",
    "static": "^2.0.0"
  }
}

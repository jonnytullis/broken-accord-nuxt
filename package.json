{
  "name": "broken-accord",
  "version": "1.0.0",
  "description": "Broken Accord Official Website",
  "author": "jonathantullis",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt build && nuxt generate",
    "deploy:static-website": "push-dir --dir=dist --branch=static-website --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.0.0",
    "vue-click-outside": "^1.1.0"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.x.x",
    "static": "^2.0.0"
  }
}
